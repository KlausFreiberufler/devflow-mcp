{
  "mcpServers": {
    "devflow": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "DEVFLOW_URL": "${user_config.devflow_url:-https://api.app.dev-flow.tech}",
        "DEVFLOW_CLIENT": "claude"
      }
    }
  }
}
